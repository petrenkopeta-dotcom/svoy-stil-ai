import React from"react";import{AccessibleDialog}from"./AccessibleDialog.js";import"./GuestAvatarPreview.css";
export function GuestAvatarPreview({avatarIndex,onAvatarChange,onCreateWardrobe,onClose}){return <AccessibleDialog className="guest-avatar-preview" labelledBy="guest-avatar-title" describedBy="guest-avatar-description" onClose={onClose}><button className="guest-avatar-preview__close" aria-label="Закрыть" onClick={onClose}>×</button><p>ПРЕДПРОСМОТР ПРОФИЛЯ</p><h2 id="guest-avatar-title">Выберите образ своего стилиста</h2><p id="guest-avatar-description">Аватар сохраняется только в этом браузере. Он не создаёт аккаунт и не открывает личные вещи.</p><div role="radiogroup" aria-label="Аватар 01–09" className="guest-avatar-preview__grid">{Array.from({length:9},(_,i)=>i+1).map(index=><button type="button" role="radio" aria-checked={avatarIndex===index} key={index} onClick={()=>onAvatarChange(index)}>№{String(index).padStart(2,"0")}</button>)}</div><p>После демо-образа создайте личное пространство, чтобы добавлять вещи, сохранять образы и обучать стилиста.</p><button type="button" className="primary" onClick={onCreateWardrobe}>Создать мой гардероб</button></AccessibleDialog>}
