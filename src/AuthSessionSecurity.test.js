import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";import {safeAuthAuditEvent} from "./authAudit.js";
test("auth audit allowlist removes PII, OTP and tokens",()=>{const event=safeAuthAuditEvent({event:"otp_verify",result:"invalid",occurredAt:"now",correlationId:"c1",userId:"opaque",email:"a@b.co",otp:"123456",accessToken:"secret"});assert.deepEqual(Object.keys(event),["event","result","occurredAt","correlationId","userId"]);});
test("personal entity RLS spec covers owner policies and private photo contract",()=>{const sql=fs.readFileSync(new URL("../supabase/migrations/20260818_personal_entities_rls.sql",import.meta.url),"utf8");assert.match(sql,/auth\.uid\(\).*user_id/);assert.match(sql,/private bucket/);assert.match(sql,/300 seconds/);});
