{
  "name": "svoy-stil-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Privacy-first local pilot of an AI wardrobe stylist",
  "type": "module",
  "engines": {
    "node": ">=22 <25",
    "npm": ">=10"
  },
  "packageManager": "npm@10.9.2",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test src/*.test.js server/*.test.js",
    "auth:bff": "node server/authBff.mjs",
    "auth:diagnose": "node scripts/auth-config-diagnostics.mjs",
    "preview": "vite preview",
    "repo:check": "node scripts/check-repository-boundary.mjs",
    "bundle:check": "node scripts/check-bundle-budget.mjs",
    "verify": "npm run repo:check && npm run format:check && npm test && npm run build && npm run bundle:check",
    "format:check": "prettier --check package.json vite.config.js server/authBff.mjs src/AuthBffServer.test.js scripts/check-bundle-budget.mjs .github/**/*.yml"
  },
  "dependencies": {
    "@vitejs/plugin-react": "6.0.4",
    "lucide-react": "1.27.0",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "vite": "8.1.5"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
