import { existsSync, readFileSync } from "node:fs";import path from "node:path";import { diagnoseAuthConfig } from "../src/auth/authConfig.js";
const file=path.resolve(import.meta.dirname,"..",".env.local");const values={};if(existsSync(file))for(const line of readFileSync(file,"utf8").split(/\r?\n/)){const match=line.match(/^([A-Za-z_][A-Za-z0-9_]*)=(.*)$/);if(match)values[match[1]]=match[2].trim().replace(/^['"]|['"]$/g,"");}const result=diagnoseAuthConfig({url:values.VITE_SUPABASE_URL||process.env.VITE_SUPABASE_URL,publishableKey:values.VITE_SUPABASE_PUBLISHABLE_KEY||process.env.VITE_SUPABASE_PUBLISHABLE_KEY});console.log(JSON.stringify({configured:result.configured,urlPresent:result.urlPresent,keyPresent:result.keyPresent,reason:result.reason},null,2));process.exitCode=result.configured?0:2;
